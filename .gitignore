build/
build-*/
deploy/
*.ymind
*.sh
# CI helper scripts must be tracked even though *.sh is ignored by default
!.github/scripts/*.sh
.cache/*
*.zip
out/
templates/
# Built-in templates and themes baked into the app via Qt resources, and the
# website's bundled themes directory, must all stay tracked even though
# `templates/` is otherwise ignored.
!resources/templates/
!resources/templates/**
!resources/themes/
!resources/themes/**
!website/public/themes/
!website/public/themes/**
CLAUDE.md
*.bat
website/node_modules/
website/dist/