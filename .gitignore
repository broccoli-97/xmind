build/
build-*/
deploy/
*.ymind
*.sh
# CI helper scripts must be tracked even though *.sh is ignored by default
!.github/scripts/*.sh
.cache/*
*.zip
out/
templates/
# Built-in templates baked into the app via Qt resources (resources/templates/)
# and the website's bundled-templates directory must both be tracked.
!resources/templates/
!resources/templates/**
!website/public/templates/
!website/public/templates/**
CLAUDE.md
*.bat
website/node_modules/
website/dist/